#include <stdio.h>
/**
 * main - A program that prints the alphet in lowercase, follow by a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
